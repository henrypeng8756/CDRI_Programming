# TODO
numList = set()
while True:
    n = eval(input())
    if n != -9999:
        numList.add(n)
    else:
        print(f'Length: {len(numList)}')
        print(f'Max: {max(numList)}')
        print(f'Min: {min(numList)}')
        print(f'Sum: {sum(numList)}')


"""
Length: _
Max: _
Min: _
Sum: _
"""
import numpy as np
